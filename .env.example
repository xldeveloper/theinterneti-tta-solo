# TTA-Solo Environment Configuration
#
# Copy this file to .env and customize as needed:
#   cp .env.example .env
#
# SECURITY WARNING: The default passwords below are for local development only!
# Always use strong, unique passwords for any non-local environment.

# =============================================================================
# Dolt Configuration (Git-like SQL database)
# =============================================================================
DOLT_HOST=localhost
DOLT_PORT=3306
DOLT_USER=root
DOLT_PASSWORD=doltpass  # CHANGE IN PRODUCTION!
DOLT_DATABASE=tta_solo

# =============================================================================
# Neo4j Configuration (Graph + Vector database)
# =============================================================================
NEO4J_HOST=localhost
NEO4J_BOLT_PORT=7687
NEO4J_HTTP_PORT=7474
NEO4J_USER=neo4j
NEO4J_PASSWORD=neo4jpass  # CHANGE IN PRODUCTION!
NEO4J_DATABASE=neo4j

# =============================================================================
# LLM Provider (for NPC AI and narrative generation)
# =============================================================================
# Uncomment and set your preferred provider:

# OpenAI
# OPENAI_API_KEY=sk-...

# Anthropic
# ANTHROPIC_API_KEY=sk-ant-...

# Local (Ollama)
# OLLAMA_HOST=http://localhost:11434
# OLLAMA_MODEL=llama3

# =============================================================================
# Development Settings
# =============================================================================
# Set to "development" or "production".
# NOTE: Currently reserved for future use; the engine does not yet read TTA_ENV.
TTA_ENV=development

# Enable verbose logging (reserved for future use)
TTA_DEBUG=true
